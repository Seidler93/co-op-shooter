# UI Reference Website

This folder is a separate React + Vite site for the Project Z menu mockups.

## Run locally

```powershell
cd ui-reference
npm install
npm run dev
```

## Build

```powershell
cd ui-reference
npm run build
```

The output is written to `ui-reference/dist`.
